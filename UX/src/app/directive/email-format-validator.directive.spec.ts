import { EmailFormatValidatorDirective } from './email-format-validator.directive';

describe('EmailFormatValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailFormatValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
